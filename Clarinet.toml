[project]
name = "stable-deserialize-optimizer"
authors = ["Blockchain Optimization Team"]
description = "Advanced data access and deserialization optimization library for Stacks smart contracts"
telemetry = false
requirements = []
[contracts.data-access-optimizer]
path = "contracts/data-access-optimizer.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
